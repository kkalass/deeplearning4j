
description = 'cdh4'
dependencies {
  compile project(':deeplearning4j-scaleout:deeplearning4j-scaleout-api')
    compile group: 'org.apache.avro', name: 'avro', version:'1.7.1'
    compile group: 'org.apache.avro', name: 'avro-ipc', version:'1.7.1'
    compile group: 'org.apache.hadoop', name: 'hadoop-common', version:'2.0.0-cdh4.6.0'
    compile group: 'org.apache.hadoop', name: 'hadoop-hdfs', version:'2.0.0-cdh4.6.0'
    testCompile group: 'org.apache.hadoop', name: 'hadoop-hdfs', version:'2.0.0-cdh4.6.0'
    testCompile group: 'org.apache.hadoop', name: 'hadoop-yarn-server-tests', version:'2.0.0-cdh4.6.0', classifier:'tests'
    testCompile group: 'org.nd4j', name: 'nd4j-jblas', version:'0.0.3.5.5.3-SNAPSHOT'
    compile(group: 'org.apache.hadoop', name: 'hadoop-yarn-api', version:'2.0.0-cdh4.6.0') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'org.apache.hadoop', name: 'hadoop-yarn-common', version:'2.0.0-cdh4.6.0') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'org.apache.hadoop', name: 'hadoop-mapreduce-client-core', version:'2.0.0-cdh4.6.0') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
}
